name: Build & Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact: zk-ui-linux
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: zk-ui-macos-arm64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: zk-ui-macos-x86_64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: zk-ui-windows

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry & build
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ matrix.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      # ── Linux: create .tar.gz ──
      - name: Package Linux
        if: runner.os == 'Linux'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/zk-ui dist/
          tar -czf ${{ matrix.artifact }}.tar.gz -C dist .

      # ── macOS: create .app bundle ──
      - name: Package macOS
        if: runner.os == 'macOS'
        run: |
          mkdir -p dist/zk-ui.app/Contents/MacOS
          mkdir -p dist/zk-ui.app/Contents/Resources
          cp target/${{ matrix.target }}/release/zk-ui dist/zk-ui.app/Contents/MacOS/
          cp Info.plist dist/zk-ui.app/Contents/
          zip -r ${{ matrix.artifact }}.app.zip dist/zk-ui.app

      # ── Windows: create .zip ──
      - name: Package Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Copy-Item target/${{ matrix.target }}/release/zk-ui.exe dist/
          Compress-Archive -Path dist/* -DestinationPath ${{ matrix.artifact }}.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: |
            *.tar.gz
            *.app.zip
            *.zip

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/**/*
          generate_release_notes: true
